# Spring configurations
spring:
  profiles:
    active: "´prod"
  main:
    allow-bean-definition-overriding: false
  security:
    require-ssl: false
  # Database
  datasource:
    url: jdbc:postgresql://containers-us-west-101.railway.app:5756/railway
    username: postgres
    password: xtzZHRl0SGsDmJJz6cEq

  # JPA properties
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    generate-ddl: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  # Server configuration
  server:
    port: 8080
    ssl:
      enabled: false
